# Database Configuration (Railway PostgreSQL)
# Get the DATABASE_URL from your Railway PostgreSQL service
DATABASE_URL=postgresql://user:password@host:port/database

# The game works without a database - scores are saved locally in the browser.
# Add a DATABASE_URL to enable a shared online leaderboard.
